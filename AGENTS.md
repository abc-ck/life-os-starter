# agents

Tool-agnostic entry point for any AI agent working in this vault.

Read `os/me.md` and follow its startup instructions. If onboarding is incomplete, read `START-HERE.md` and `os/onboarding-interview.md` before changing the structure.

Durable identity, working rules, vault structure, retrieval, and skills live in `os/`. Tool-specific files stay thin so the owner can switch models without losing continuity.
